services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cor
      POSTGRES_PASSWORD: cor_secret
      POSTGRES_DB: cor_explorer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
